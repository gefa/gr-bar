# auto-generated by grc.converter

id: bar_wireshark_connector
label: Wireshark Connector
category: '[Bar]'

parameters:
-   id: tech
    label: Technology
    dtype: enum
    default: '127'
    options: ['127', '195']
    option_labels: [WiFi, ZigBee]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]

inputs:
-   domain: message
    id: in

outputs:
-   domain: stream
    dtype: byte
    multiplicity: '1'

templates:
    imports: import bar
    make: bar.wireshark_connector(${tech}, ${debug})

file_format: 1
