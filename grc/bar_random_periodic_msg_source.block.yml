# auto-generated by grc.converter

id: bar_random_periodic_msg_source
label: Random Periodic Message Source
category: '[Bar]'

parameters:
-   id: msg_len
    label: Message Length
    dtype: int
    default: '64'
-   id: interval
    label: Period (ms)
    dtype: int
    default: '1000'
-   id: quit
    label: Quit
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: num
    label: Num Messages
    dtype: int
    default: '1'
-   id: seed
    label: Random Seed
    dtype: int
    default: '1'

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: |-
        import bar
        import pmt
    make: bar.random_periodic_msg_source(${msg_len}, ${interval}, ${num}, ${quit},
        ${debug}, ${seed})

file_format: 1
