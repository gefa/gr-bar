# auto-generated by grc.converter

id: bar_packet_dropper
label: Packet Dropper
category: '[Bar]'

parameters:
-   id: drop_rate
    label: Packet drop rate
    dtype: real
    default: '0.5'
-   id: seed
    label: Random seed
    dtype: int
    default: '42'

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import bar
    make: bar.packet_dropper(${drop_rate}, ${seed})

file_format: 1
